export enum TaskPrivacy {
  PUBLIC = 'public',
  PRIVATE = 'private',
  GROUP = 'group',
}

export enum TaskColor {
  MAIN_TASK = '#9799cd',
  SUB_TASK = '#eab3b6',
}

export enum TaskType {
  MAIN_TASK = 'main',
  SUB_TASK = 'sub',
}
